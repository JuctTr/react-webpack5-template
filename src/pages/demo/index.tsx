import React, { useState, useRef, useCallback } from 'react';
import './index.scss';

export default function Index() {
    return <div className="page">Hello React + Webpack5 + JavaScript + Demo</div>;
}
